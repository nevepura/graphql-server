type Query {
  bookById(id: ID): Book
  booksByGenre(genre: String): [Book]
  authorById(id: ID): Author

}


type Book {
  id: ID
  name: String
  pageCount: Int
  author: Author
  genre: String
}

type Author {
  id: ID
  firstName: String
  lastName: String
  active: Boolean
}
